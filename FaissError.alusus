import "libfaiss_c.so";

@merge module Faiss {

def FaissMetricType {
        OK = 0,
        UNKNOWN_EXCEPT = -1,
        FAISS_EXCEPT = -2,
        STD_EXCEPT = -4,
    }
@expname[faiss_get_last_error]
func getLastError(): CharsPtr;
}

